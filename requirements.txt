# ComfyUI Motion Transfer Requirements

# Core dependencies
torch>=2.2.0  # Upgraded for SEA-RAFT compatibility
torchvision>=0.15.0
numpy>=1.24.0
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0  # For guided filter (ximgproc)

# Optical flow models
# Original RAFT (install from source):
# pip install git+https://github.com/princeton-vl/RAFT.git

# SEA-RAFT (recommended - faster and more accurate):
# pip install git+https://github.com/princeton-vl/SEA-RAFT.git

# HuggingFace Hub (for automatic SEA-RAFT model downloads)
huggingface-hub>=0.20.0

# Image I/O
Pillow>=10.0.0
imageio>=2.31.0

# High-res file formats (optional but recommended)
# Note: EXR support is optional - if installation fails, you can still use PNG/JPG output
# OpenEXR 3.x includes Imath bundled, no separate install needed
OpenEXR>=1.3.0  # For 16K EXR export

# Mesh processing (for Pipeline B)
scipy>=1.11.0  # For Delaunay triangulation

# Depth estimation (for Pipeline C - optional)
# timm>=0.9.0  # For DPT/MiDaS models (uncomment if using real depth estimation)

# Utilities
tqdm>=4.66.0  # Progress bars
